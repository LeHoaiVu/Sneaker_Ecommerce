import { createAction } from 'redux-actions';

export const showRegisterModal = createAction('SHOW_REGISTER_SUCCESS_MODAL');
export const hideRegisterModal = createAction('HIDE_REGISTER_SUCCESS_MODAL');
